<%#= link_to 'イベント追加', new_kintaikun_even_path %>
<%=link_to 'もどる', root_path, class: :nav__btn%>
<%= month_calendar events: @events do |date, events| %>
  <%= date.day %>

<%# ここから追記 %>
  <% events.each do |event| %>
    <div>
      <%= event.title %>
    </div>
  <% end %>
<%# ここまで追記 %>
<% end %>